# Using transfer learning and tried to implement a project
#( NO FINE_TUNING,NO tf.keras.applications) 
## USed the tensorflow hub(Specifically the efficientnetB2 and the Resnet50 pretrained models)
# Extracted the above mentioned models and used them as a feature extractor layer. 